{
  "name": "swastyaconnect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && cp -r frontend/dist public",
    "vercel-build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && cp -r frontend/dist public"
  }
}
